# Mobile CI - DISABLED TEMPORARILY
# Re-enable when ready to integrate mobile app CI/CD
# Uncomment the entire workflow below to enable

# name: Mobile CI
# on:
#   push:
#     branches: [ main, develop ]
#     paths:
#       - 'attendance-mobile/**'
#       - '.github/workflows/mobile-ci.yml'
#   pull_request:
#     branches: [ main, develop ]
#     paths:
#       - 'attendance-mobile/**'
# 
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
# 
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#           cache-dependency-path: attendance-mobile/package-lock.json
# 
#       - name: Setup Expo
#         uses: expo/expo-github-action@v8
#         with:
#           expo-version: latest
#           token: ${{ secrets.EXPO_TOKEN }}
# 
#       - name: Install dependencies
#         working-directory: ./attendance-mobile
#         run: npm ci
# 
#       - name: Run TypeScript check
#         working-directory: ./attendance-mobile
#         run: npx tsc --noEmit
# 
#   build-android:
#     runs-on: ubuntu-latest
#     needs: test
#     if: github.ref == 'refs/heads/main'
#     
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
# 
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#           cache-dependency-path: attendance-mobile/package-lock.json
# 
#       - name: Setup EAS
#         uses: expo/expo-github-action@v8
#         with:
#           expo-version: latest
#           token: ${{ secrets.EXPO_TOKEN }}
#           eas-version: latest
# 
#       - name: Install dependencies
#         working-directory: ./attendance-mobile
#         run: npm ci
# 
#       - name: Build Android APK
#         working-directory: ./attendance-mobile
#         run: eas build --platform android --profile preview --non-interactive
# 
#       - name: Build Android AAB (Production)
#         working-directory: ./attendance-mobile
#         if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#         run: eas build --platform android --profile production --non-interactive
