# Deployment workflow - TEMPORARILY DISABLED
# Re-enable after YAML formatting issues are resolved

# name: Deploy to Production
# on:
#   workflow_dispatch:
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Deploy to VPS
#         uses: appleboy/ssh-action@master
#         with:
#           host: ${{ secrets.VPS_HOST }}
#           username: ${{ secrets.VPS_USERNAME }}
#           password: ${{ secrets.VPS_PASSWORD }}
#           script: cd /var/www/version2_attendance && git fetch origin main && git reset --hard origin/main && npm install && cd backend && npm install && npx prisma generate && pm2 restart v2_attendance-api --update-env && cd ../frontend && npm install && echo "NEXT_PUBLIC_API_URL=https://attendacev2.xandree.com/api" > .env.local && pm2 restart v2_attendance-web --update-env && pm2 status
